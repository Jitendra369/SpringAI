
services:
  aiservice:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-service
    ports:
      - "8859:8859"
    restart: always
#    environment:
#      - OPENAI_API_KEY=${OPENAI_API_KEY}